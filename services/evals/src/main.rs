/// evals service entrypoint.
///
/// Responsibilities:
/// - Run evaluation pipelines and scoring.
/// - Emit eval metrics to analytics backends.
/// - Provide regression reports for agent runs.
fn main() {
    println!("evals bootstrapped; evaluation pipeline wiring pending");
}
