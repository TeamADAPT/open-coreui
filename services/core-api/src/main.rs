/// core-api service entrypoint.
///
/// Responsibilities:
/// - Accept inbound requests and validate payloads.
/// - Route requests into the agent-orchestrator workflow layer.
/// - Expose collaboration endpoints for checkpoints, approvals, and comments.
fn main() {
    println!("core-api bootstrapped; wiring to Temporal and routing layer pending");
}
